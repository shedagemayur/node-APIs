const config = {
    DATABASE: {
        host: 'localhost',
        user: 'root',
        database: 'atomchat',
        waitForConnections: true,
        connectionLimit: 1,
        queueLimit: 0
    }
}

module.exports = config;